import React from "react";

const Hero: React.FC = () => {
  return (
    <section className="relative w-full h-screen min-h-[700px] mx-auto flex justify-center items-center">
      <h1 className="text-[48px]">Nested Nav Bar</h1>
    </section>
  );
};

export default Hero;
